<antd:Window
    x:Class="AntdDemo.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:antd="clr-namespace:Antd.Controls;assembly=antd"
    xmlns:local="using:AntdDemo.Views"
    mc:Ignorable="d"
    Width="930"
    Height="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="65" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Background="#f5f5f5" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Button x:Name="Change" Content="Change" Width="65" Height="35" />
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Bottom" Grid.Column="0" Grid.ColumnSpan="2">
            <Button Content="Change" Height="35" />
        </StackPanel>    
        
    </Grid>
    
</antd:Window>
